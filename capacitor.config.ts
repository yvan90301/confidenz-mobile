import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.confidezn.app',
  appName: 'confidenz',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
